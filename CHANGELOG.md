# DeepWork Desktop Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [1.0.0] - 2026-03-31
### Added
- **Core Hardware Engine**: Completely natively-built zero-driver performance monitor leveraging PDH and DXGI for Sub-1% CPU usage.
- **Dynamic Dual-Widget Anchoring**: A unique modular layout cleanly dividing System Hardware and Analytics into separate `HWND_TOPMOST` layered bounds explicitly immune to DWM `Win+D` (Show Desktop) interruptions. 
- **Focus Engine (Token Economy)**: The primary algorithm mapping "Good Jobs" (Visual Studio, Blenders, Terminals) equivalently at 1 token-per-second intervals natively to your localized session.
- **Social & Media Multipliers**: Browser URL string parsing directly via `UIAutomationCore` explicitly detects and differentiates Social Media (4x drain multiplier) and generic Media (3x drain multiplier) dynamically natively inside Chrome, Edge, Brave, and Firefox.
- **Native Anti-Cheat**: 
  - **Retroactive Heartbeat**: Subsystem natively unwinds the Token Ledger implicitly evaluating AFK time via 90-second bounds to eliminate "Thinking Penalty".
  - **Bounding Box Banishment**: Rolling structural coordinate array natively evaluates continuous physical mouse locations, neutralizing pixel jigglers unconditionally. 
- **The Tab Assasin**: Aggressively mimics absolute keystroke structures natively injecting `CTRL + W` directly into the foreground browser window when localized token limits deplete without warning to definitively vaporize distraction windows safely. 
- **Encrypted Persistence**: XOR-hashed dynamic binary arrays strictly encrypt and validate the entire analytics dashboard into `assets\engine_state.bin` automatically at exact 1-minute pulse intervals.
- **Custom Inno Setup Scripts**: Deployment configurations formally instantiated natively to safely pack and auto-install DeepWork Desktop across modern environments natively including automatic start-up registry injection hooks.
